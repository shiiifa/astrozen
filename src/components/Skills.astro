---
import Section from "./Section.astro";
import type { SkillProps } from "@types";

interface Props {
  skills: SkillProps[];
}

const { skills } = Astro.props;

// Vector icons for each skill category (using emojis as placeholders)
const skillIcons: Record<string, string> = {
  "Technical Skills": "⚙",    // Gear icon for technical skills
  "Design & Creative Skills": "🖌",       // Art palette icon for design skills  
  "Professional & Collaboration Skills": "𓂫"  // Handshake icon for professional skills
};
---

<Section text="skills" href="skills">
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6 md:gap-4">
    {skills.map((skill) => (
      <div class="fade-in-element text-center">
        {/* Icon */}
        <div class="text-3xl mb-3 text-white">{skillIcons[skill.category]}</div>
        
        {/* Category Title */}
        <h3 class="font-serif text-lg font-semibold text-primary mb-3">
          {skill.category}
        </h3>
        
        {/* Skills List - Center aligned text */}
        <div class="text-neutral text-xs leading-relaxed text-center">
          {skill.tools.split('\n').map((tool) => (
            <div class="mb-2" set:html={tool.replace(/^([^:]+):/, '<strong>$1:</strong>')} />
          ))}
        </div>
      </div>
    ))}
  </div>
</Section>