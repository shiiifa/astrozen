---
import Section from "./Section.astro";
import type { ExperienceProps } from "@types";

interface Props {
  experience: ExperienceProps[];
}

const { experience } = Astro.props;
---

<Section text="experience" href="experience">
  {
    experience.map(({ company, position, startDate, endDate, summary }) => (
      <div class="mb-8">
        <h3 class="mb-1 font-serif text-xl font-semibold text-white">
          {company}
        </h3>
        <div class="flex flex-col items-start pb-4">
          <h4 class="mb-0.5 font-serif text-lg font-medium text-primary">
            {position}
          </h4>
          <span class="pb-[2px] text-xs text-white/70">
            {startDate} — {endDate}
          </span>
        </div>
        {Array.isArray(summary) ? (
          <ul class="list-none">
            {summary.map((log) => (
              <li class="relative mb-2 pl-6 text-sm text-neutral before:absolute before:top-1 before:left-0 before:content-[url(/check.svg)]">
                {log}
              </li>
            ))}
          </ul>
        ) : (
          <p class="text-sm text-neutral">{summary}</p>
        )}
      </div>
    ))
  }
</Section>